# Anthropic Python SDK.
# Pinned below 1.0: openinference-instrumentation-anthropic 1.1.2 imports
# anthropic.resources.completions, which 1.x removed, so instrument() raises
# ModuleNotFoundError. Drop the upper bound once the instrumentor supports 1.x.
anthropic>=0.40.0,<1.0

# Environment
python-dotenv>=1.0.0

# OpenTelemetry / OpenInference instrumentation (optional — for Lumoz tracing)
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-http>=1.20.0
openinference-instrumentation>=0.1.0
openinference-instrumentation-anthropic>=1.1.0
